apiVersion: v1
kind: Service
metadata:
  name: {{ include "app101-web.fullname" . }}
  labels:
    {{- include "app101-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "app101-web.selectorLabels" . | nindent 4 }}
