apiVersion: v1
kind: Service
metadata:
  name: {{ include "app101-api-py.fullname" . }}
  labels:
    {{- include "app101-api-py.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "app101-api-py.selectorLabels" . | nindent 4 }}
