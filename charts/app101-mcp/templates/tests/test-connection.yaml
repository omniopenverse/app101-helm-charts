apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "app101-mcp.fullname" . }}-test-connection"
  labels:
    {{- include "app101-mcp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "app101-mcp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
